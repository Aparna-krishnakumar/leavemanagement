@{
    ViewBag.Title = "Apply Leave";
}
<div class="jumbotron"><h1>Apply Leave</h1></div>
<div class="justify-content-center row">
    <div class="col-sm-6">
        
        
        <form novalidate="novalidate" action="/leave/AddLeaveAdmin" method="post" class="needs-validation" enctype="multipart/form-data">
            @Html.AntiForgeryToken()
            <div class="form-row">
                <div class="form-group col-sm-6">
                    <label for="txtStartDate">Start Date</label>
                    <input type="date" class="form-control" id="txtStartDate" placeholder="Enter Start Date" name="StartDate" required="required" autofocus="autofocus" />

                </div>

                <div class="form-group col-sm-6">
                    <label for="txtEndDate">End Date</label>
                    <input type="date" class="form-control" id="txtEndDate" placeholder="Enter End Date" name="EndDate" required="required" />

                </div>
            </div>
            <div class="form-row">
                <div class="form-group col-sm-12">
                    <label for="txtReason">Reason Of Absence</label>
                    <textarea type="text" class="form-control" id="txtReason" placeholder="Reason Of Absence" name="ReasonOfAbsence" required="required"></textarea>

                </div>


            </div>



            <button type="submit" class="btn btn-primary">Apply</button>
            @if ((string)Session["CuurentUserDesignation"] == "HR")
            {
                <a class="btn btn-danger float-right" href="~/HR/index">Cancel</a>
            }
            else if((string)Session["CuurentUserDesignation"] == "Project Manager")
            {
                <a class="btn btn-danger float-right" href="~/ProjectManager/index">Cancel</a>
            }
           
        </form>
    </div>
</div>

<script>

    $("a[href='/leave/addleaveadmin']").attr("id", "active");

    $(document).on("submit", ".needs-validation", validate);
    $(".needs-validation input").blur(validate);
    function validate(event)
    {
        if ($(".needs-validation")[0].checkValidity() == false || $(".email-exist").text() != "")
        {
            event.preventDefault();
        }
        $(".needs-validation")[0].classList.add("was-validated");
    }


</script>